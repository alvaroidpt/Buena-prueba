import { BaseRecord, BaseResource } from 'adminjs';
import { ExporterType } from './exporter.type.js';
export type Exporter = (records: BaseRecord[]) => string;
export type Importer = (records: string, resource: BaseResource) => Promise<BaseRecord[]>;
export declare const Parsers: Record<ExporterType, {
    export: Exporter;
    import: Importer;
}>;
